[pytest]
# Look for tests under backend/tests
testpaths = backend/tests

# Make 'backend/' a root of imports so 'import app...' works
pythonpath = backend

# Don’t recurse into the venv directory
norecursedirs = .venv