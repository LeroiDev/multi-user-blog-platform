version: "3.8"
services:
  backend:
    # will build FastAPI service here
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    # will build React service here
    build: ./frontend
    ports:
      - "3000:3000"
